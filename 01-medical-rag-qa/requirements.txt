# 01 · 医疗 RAG —— 运行依赖
# 底座(Python 3.10 / CUDA 12.1 / torch 2.3.x cu121)见仓库根 ENVIRONMENT.md，
# torch 用 cu121 wheel 单独装，不写死在这里以免覆盖。

python-dotenv

# LangChain 1.x（代码使用 langchain_classic 命名空间，需 1.0+）
langchain>=1.0
langchain-classic
langchain-community
langchain-core
langchain-text-splitters
langchain-milvus

pymilvus>=2.4.6
zai-sdk            # 智谱 SDK：from zai import ZhipuAiClient
openai>=1.40       # DeepSeek 走 OpenAI 兼容接口

fastapi>=0.111
uvicorn>=0.30
pydantic>=2.8

pandas
openpyxl
pdfplumber
tqdm
rank-bm25
FlagEmbedding
sentence-transformers>=3.0

# 本地 Qwen2.5-32B 推理（USE_LOCAL_LLM=1 时需要）
transformers>=4.44
accelerate>=0.33
bitsandbytes>=0.43
