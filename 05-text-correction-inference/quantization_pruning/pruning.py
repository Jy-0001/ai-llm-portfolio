#模型剪枝
    #定义：仿照⽣物的稀疏神经⽹络, 将⼤型⽹络中的稠密连接变成稀疏的连接, 并同样达到SOTA的效果, 就是模型剪枝的原动⼒.
    #方式：
        #对特定⽹络模块的剪枝(Pruning Model).
        # 多参数模块的剪枝(Pruning multiple parameters).
        # 全局剪枝(GLobal pruning).
        # ⽤户⾃定义剪枝(Custom pruning).